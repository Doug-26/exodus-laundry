# Angular 22 requires TypeScript 6.0, but typescript-eslint 8.x still declares a
# peer range of <6.0.0. The combo works at runtime (ng lint passes), so we relax
# peer resolution until typescript-eslint ships official TS 6.0 support.
legacy-peer-deps=true
