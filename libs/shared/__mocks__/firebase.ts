/** Minimal Firebase stub for Jest. Models import Timestamp as a type only; no runtime needed. */
export class Timestamp {
  constructor(
    public readonly seconds: number,
    public readonly nanoseconds: number,
  ) {}
}
